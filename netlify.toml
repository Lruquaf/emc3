[build]
  base = "apps/web"
  command = "cd ../.. && pnpm install && pnpm build:web"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9.0.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
